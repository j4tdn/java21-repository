package view.functional.iinterface;

public interface IntComparator {

	//hàm dưới là hàm trừu tượng
	int compare(int a, int b);
	
	//Để tạo một đối tượng cho IntComparator 
	// --> Dùng external class, anonymous class
	//
}
