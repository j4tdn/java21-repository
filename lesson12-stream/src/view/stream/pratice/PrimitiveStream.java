package view.stream.pratice;

public class PrimitiveStream {

	public static void main(String[] args) {
		//  Primitive Stream: IntStream, DoubleStream, LongStream
		// --> 100% chưa các phần tử KDL nguyên thuỷ: int, double, long
		// --> hỗ trợ các hàm giống như trong Stream<T> nhưng thay vì truyền T biết được là 
		// --> hỗ trợ thêm các hàm cho KDL nguyên thuỷ sum, sub, mul, agv, div
		
		// IntStream
		// forEach(IntConsumer consumer)
		
		// Object Stream: Stream<T>
		// forEach(Consumer<T> consumer)
		
		// int[] numbers = {1,2,3};
		// Arrays.stream(numbers) // IntStream
		
		
		// Integer[] digits = {1,2,3};
		
	}
}
